This code sample uses the InfiniteGraph database to analyze data from disparate datasources. Three relational databases have been exported to CSV (located in the resources directory). The merged graph is accessed from Tom Sawyer for visualization. 

Instructions:
1. Install the free evaluation of InfiniteGraph
2. Install maven
3. mvn compile
4. mvn exec:exec (launches TomSawyer / IG viz and ingest) 
